// ===== LboCraft Cloud Config =====

const LBOCRAFT_CONFIG = {
  // رابط الـ API المحلي
  API_BASE: 'api',
  // رمز المرور
  PASSCODE: '0099',

  // ===== GitHub API (للتعديلات السحابية — اختياري) =====
  // عشان التعديلات تظهر على كل الأجهزة،
  // سوّي Personal Access Token من GitHub:
  // Settings → Developer settings → Personal access tokens → Fine-grained tokens
  // الصلاحية: Contents (Read and write)
  GITHUB_TOKEN: '',
  GITHUB_REPO: 'Labeeb123otb/DRB',  // اسم المستخدم/المستودع
  GITHUB_BRANCH: 'main',
};
