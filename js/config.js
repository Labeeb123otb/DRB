// ===== LboCraft Cloud Config =====

const LBOCRAFT_CONFIG = {
  // رابط الـ API المحلي
  API_BASE: 'api',
  // رمز المرور
  PASSCODE: '0099',

  // ===== GitHub API (للتعديلات السحابية) =====
  // عشان التعديلات تظهر على كل الأجهزة:
  // 1. سجّل الدخول للوحة التحكم
  // 2. اذهب إلى الإعدادات
  // 3. الصق الـ GitHub Token في حقل "GitHub Token"
  // 4. اضغط حفظ — يتخزن في المتصفح ويشتغل السحاب
  GITHUB_TOKEN: '',
  GITHUB_REPO: 'Labeeb123otb/DRB',
  GITHUB_BRANCH: 'main',
  GITHUB_REPO: 'Labeeb123otb/DRB',  // اسم المستخدم/المستودع
  GITHUB_BRANCH: 'main',
};
