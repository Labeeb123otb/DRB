# Protect data directory
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^data/ - [F,L]
</IfModule>

# Deny direct access to data files
<FilesMatch "\.json$">
    Deny from all
</FilesMatch>
